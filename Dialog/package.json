{
  "name": "@fck-components/dialog",
  "version": "0.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "private": false,
  "scripts": {
    "copy:files": "copyfiles -u 1 src/**/*.css src/**/*.svg lib/",
    "build": "rm -rf lib && mkdir lib && npm run copy:files && tsc -p .",
    "build:watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/react": "^17.0.37",
    "copyfiles": "^2.4.1",
    "typescript": "^4.5.3"
  },
  "peerDependencies": {
    "react": ">=17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "^4.3.1",
    "typescript": ">=4.5.3"
  },
  "files": [
    "/lib"
  ]
}
